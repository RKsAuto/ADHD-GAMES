__pycache__/
*.py[cod]
.DS_Store
venv/

# local participant data (created when running without MONGO_URI)
local_data.json
local_sessions.json
*.lock
*.tmp

# session runner artifacts
session_server.log
session_tunnel.log
cloudflared

# credentials written by deploy/pi-setup.sh — never commit
deploy/adhd-games.env
